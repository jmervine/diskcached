# -*- ruby -*-

require 'rubygems'
require 'hoe'

# Hoe.plugin :compiler
# Hoe.plugin :gem_prelude_sucks
# Hoe.plugin :inline
# Hoe.plugin :racc
# Hoe.plugin :rcov
# Hoe.plugin :rubyforge

Hoe.spec 'simple_disk_cache' do
  # HEY! If you fill these out in ~/.hoe_template/Rakefile.erb then
  # you'll never have to touch them again!
  # (delete this comment too, of course)

  developer('Joshua Mervine', 'joshua@mervine.net')

  # self.rubyforge_name = 'disk_cachex' # if different than 'disk_cache'
end

task :benchmark do
  load './spec/benchmarks.rb'
end

# vim: syntax=ruby
